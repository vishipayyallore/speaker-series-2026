# Demo Script — Python Session 1

**Estimated demo time:** 15 minutes  
**Environment:** local

---

## Before you start

- [ ] Python 3.11+ available: `python --version`
- [ ] Clone [python-fundamentals-in-practice](https://github.com/{your-org}/python-fundamentals-in-practice) (or have it open)
- [ ] Terminal and editor visible side by side

---

## Demo flow

### 1. REPL and types

**Say:** Python is dynamically typed; we explore values before worrying about declarations.

**Do:**

```bash
python
```

```python
name = "Dot Net Learners"
count = 3
items = ["loops", "lists", "functions"]
```

**Show:** Types via `type(name)` and iterating `items`.

---

### 2. Small script from the companion repo

**Say:** Real work lives in files; we run one example from the practice repo.

**Do:** Open the session-01 example in the external repo and run it.

**Show:** Expected output and where attendees continue after the meetup.

---

## Fallback plan

If the external repo is unreachable, use an inline 10-line script saved locally during the session.

---

## Cleanup

- [ ] Close REPL and reset terminal if needed
