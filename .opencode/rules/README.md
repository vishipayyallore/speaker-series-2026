# Rules (OpenCode mirror)

Synced from `.cursor/rules/*.mdc` via `tools/psscripts/sync-assistant-mirrors.ps1`.

Edit canonical rules under `.cursor/rules/` first, then run sync.

See `.clinerules/rules/README.md` for the file mapping.
