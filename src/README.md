# src/

Repository-wide utilities only. **Per-talk demo code** belongs under `talks/{talk-id}/src/`, not here.

Use this folder if you later add cross-talk automation (for example, a script that regenerates the root README talk table).
